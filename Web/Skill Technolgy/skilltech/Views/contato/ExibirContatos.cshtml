@model IEnumerable<skilltech.Models.contatoModel>

@{
    ViewData["Title"] = "ExibirContatos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>ExibirContatos</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
       <!-- <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            <th></th>
         </tr>*/-->
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
         <!-- <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>-->

            <td>

                    <div action="">
                        <h5>Mensagem de:  @Html.DisplayFor(modelItem => item.Name) </h5>
                        <h6>Email:  @Html.DisplayFor(modelItem => item.Email)  </h6>
                        <h6>Telefone:   @Html.DisplayFor(modelItem => item.Telefone)</h6>
                        <article>
                            @Html.DisplayFor(modelItem => item.Message)
                        </article>
                        <hr />
                    </div>
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>

}
    </tbody>
</table>
